#!/usr/bin/env bash
set -euo pipefail

nix="$HOME/.nix-profile/bin/nix"
if [[ ! -x "$nix" ]]; then
  echo "Nix is unavailable; rerun .agents/setup" >&2
  exit 1
fi

echo "Nix is available: $("$nix" --version)"
